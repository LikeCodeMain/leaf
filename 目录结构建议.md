# 📁 Headless UI 组件库目录结构建议（Vue 专用）

## ✅ 推荐的目录结构

### 方案一：完整版（有通用逻辑）

```
leaf/
├── src/                          # 源代码目录
│   ├── composables/             # Composables（通用逻辑层）
│   │   ├── useToggle.ts         # 开关逻辑（多个组件共用）
│   │   ├── useFocus.ts          # 焦点管理（多个组件共用）
│   │   ├── useKeyboard.ts       # 键盘导航（多个组件共用）
│   │   └── index.ts             # 统一导出
│   │
│   ├── components/              # 组件目录
│   │   ├── button/              # Button 组件
│   │   │   ├── useButton.ts     # Button 逻辑 Composable
│   │   │   ├── Button.vue       # Button 组件（可选，提供默认渲染）
│   │   │   └── index.ts         # 导出
│   │   ├── input/               # Input 组件
│   │   │   ├── useInput.ts
│   │   │   ├── Input.vue
│   │   │   └── index.ts
│   │   ├── select/              # Select 组件
│   │   │   ├── useSelect.ts
│   │   │   ├── Select.vue
│   │   │   └── index.ts
│   │   └── index.ts             # 统一导出所有组件
│   │
│   ├── utils/                   # 工具函数
│   │   ├── dom.ts               # DOM 操作工具
│   │   ├── a11y.ts              # 可访问性工具
│   │   └── index.ts
│   │
│   └── index.ts                 # 主入口文件
│
├── dist/                        # 构建输出
│   ├── leaf.es.js              # ES 模块
│   ├── leaf.cjs.js             # CommonJS 模块
│   ├── leaf.umd.js             # UMD 模块
│   └── index.d.ts              # TypeScript 类型定义（自动生成）
```



## 📘 TypeScript 支持

### ✅ TypeScript 对组件库开发的好处

1. **类型安全**
   - 编译时发现错误，减少运行时 bug
   - Props 类型检查，避免传错参数
   - 返回值类型明确，IDE 自动补全更准确

2. **更好的开发体验**
   - IDE 智能提示和自动补全
   - 重构更安全，类型系统保证一致性
   - 文档即代码，类型定义就是最好的文档

3. **更好的用户体验**
   - 用户在使用组件库时也能获得类型提示
   - 即使使用 JavaScript，也能通过 JSDoc 获得提示
   - 减少用户查阅文档的时间

4. **维护性**
   - 大型组件库更容易维护
   - 类型定义帮助理解代码结构
   - 团队协作更顺畅

### 🎯 使用 TypeScript 的知名组件库

以下组件库都使用 TypeScript 开发：

- **Vue 生态**：
  - [Element Plus](https://element-plus.org/) - 完全使用 TypeScript
  - [Ant Design Vue](https://antdv.com/) - TypeScript 支持
  - [Naive UI](https://www.naiveui.com/) - 完全使用 TypeScript
  - [Vuetify](https://vuetifyjs.com/) - TypeScript 支持
  - [Quasar](https://quasar.dev/) - TypeScript 支持

- **React 生态**：
  - [Ant Design](https://ant.design/) - 完全使用 TypeScript
  - [Material-UI (MUI)](https://mui.com/) - TypeScript 优先
  - [Chakra UI](https://chakra-ui.com/) - TypeScript 支持
  - [Radix UI](https://www.radix-ui.com/) - TypeScript 优先

- **Headless UI 组件库**：
  - [Headless UI](https://headlessui.com/) - TypeScript 支持
  - [React Aria](https://react-spectrum.adobe.com/react-aria/) - TypeScript 优先

### ❓ 用户必须使用 TypeScript 吗？

**答案：不需要！**

- ✅ **组件库用 TypeScript 开发** → 编译后生成 `.d.ts` 类型定义文件
- ✅ **用户用 JavaScript** → 可以直接使用，只是没有类型提示
- ✅ **用户用 TypeScript** → 自动获得完整的类型提示和检查

**工作原理：**

```typescript
// 组件库源码（TypeScript）
// src/components/button/useButton.ts
export function useButton(props: ButtonProps) {
  // ...
}

// 构建后生成
// dist/leaf.es.js          ← JavaScript 代码（用户可以直接使用）
// dist/index.d.ts          ← 类型定义文件（TypeScript 用户自动获得类型）
```

**用户使用示例：**

```javascript
// 用户项目：JavaScript（完全可以使用）
import { Button } from 'leaf';

// 用户项目：TypeScript（获得类型提示）
import { Button } from 'leaf';
// ↑ IDE 会自动提示 Button 的 props 类型
```

## 🤔 目录结构说明

### `composables/` vs `components/` 的区别

- **`composables/`**：**通用逻辑**（如 `useToggle`, `useFocus`, `useKeyboard`）
  - 多个组件可能共用的基础逻辑
  - 例如：Button、Switch、Checkbox 都可能用到 `useToggle`
  
- **`components/`**：**具体组件**（如 `Button`, `Input`, `Select`）
  - 每个组件有自己的逻辑（`useButton.js`）和渲染（`Button.vue`）
  - 组件逻辑可能使用通用 composables

### 💡 如果不需要通用逻辑，可以简化

如果你的组件逻辑都是独立的，不需要共享通用逻辑，可以**去掉 `composables/` 目录**，只保留 `components/`：

```
src/
├── components/              # 所有组件都在这里
│   ├── button/
│   │   ├── useButton.js    # Button 自己的逻辑
│   │   ├── Button.vue
│   │   └── index.js
│   ├── input/
│   │   ├── useInput.js
│   │   ├── Input.vue
│   │   └── index.js
│   └── index.js
└── index.js
```

## 🎯 核心设计原则

### 1. **逻辑与渲染分离**

```
components/button/
├── useButton.js    ← 纯逻辑 Composable（状态、事件处理）
└── Button.vue      ← 渲染层（可选，提供默认实现）
```

### 2. **Composable 优先**

- 每个组件的逻辑封装在自己的 `useXxx.js` 中
- 组件使用 Composable，但样式完全由开发者控制
- 提供默认组件实现，但不强制使用

### 3. **统一导出**

```typescript
// src/index.ts
export { useButton } from './components/button';
export { Button } from './components/button';
export { useInput } from './components/input';
export { Input } from './components/input';

// 或者按需导出
export * from './components';
export * from './composables';

// 导出类型
export type { ButtonProps } from './components/button';
export type { InputProps } from './components/input';
```

## 📝 示例：Button 组件结构（TypeScript）

### `src/components/button/types.ts`（类型定义）

```typescript
export interface ButtonProps {
  disabled?: boolean;
  type?: 'button' | 'submit' | 'reset';
  onClick?: (event: MouseEvent) => void;
  onFocus?: (event: FocusEvent) => void;
  onBlur?: (event: FocusEvent) => void;
}

export interface UseButtonReturn {
  isPressed: Ref<boolean>;
  isFocused: Ref<boolean>;
  disabled: boolean;
  type: string;
  onClick: (event: MouseEvent) => void;
  onFocus: (event: FocusEvent) => void;
  onBlur: (event: FocusEvent) => void;
  ariaAttrs: ComputedRef<{
    'aria-disabled': boolean;
    'aria-pressed': boolean;
    role: string;
  }>;
}
```

### `src/components/button/useButton.ts`（核心逻辑 Composable）

```typescript
import { ref, computed, type Ref, type ComputedRef } from 'vue';
import type { ButtonProps, UseButtonReturn } from './types';

/**
 * Button 核心逻辑 Composable
 * 纯逻辑层，处理状态和事件，不涉及渲染
 */
export function useButton(props: ButtonProps = {}): UseButtonReturn {
  const {
    disabled = false,
    type = 'button',
    onClick,
    onFocus,
    onBlur
  } = props;

  // 状态管理
  const isPressed = ref(false);
  const isFocused = ref(false);

  // 事件处理
  const handleClick = (event) => {
    if (disabled) return;
    isPressed.value = true;
    onClick?.(event);
    // 重置按下状态（可根据需求调整）
    setTimeout(() => {
      isPressed.value = false;
    }, 150);
  };

  const handleFocus = (event) => {
    isFocused.value = true;
    onFocus?.(event);
  };

  const handleBlur = (event) => {
    isFocused.value = false;
    onBlur?.(event);
  };

  // 计算属性：ARIA 属性
  const ariaAttrs = computed(() => ({
    'aria-disabled': disabled,
    'aria-pressed': isPressed.value,
    role: 'button'
  }));

  // 返回状态和事件处理器
  return {
    // 状态
    isPressed,
    isFocused,
    disabled,
    type,
    
    // 事件处理器
    onClick: handleClick,
    onFocus: handleFocus,
    onBlur: handleBlur,
    
    // ARIA 属性
    ariaAttrs
  };
}
```

### `src/components/button/Button.vue`（可选默认渲染）

```vue
<template>
  <button
    :type="type"
    :disabled="disabled"
    :aria-disabled="ariaAttrs['aria-disabled']"
    :aria-pressed="ariaAttrs['aria-pressed']"
    :role="ariaAttrs.role"
    :class="buttonClass"
    @click="onClick"
    @focus="onFocus"
    @blur="onBlur"
  >
    <slot />
  </button>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import { useButton } from './useButton';
import type { ButtonProps } from './types';

const props = withDefaults(defineProps<ButtonProps & {
  class?: string;
}>(), {
  disabled: false,
  type: 'button'
});

// 使用核心逻辑
const {
  isPressed,
  isFocused,
  disabled,
  type,
  onClick,
  onFocus,
  onBlur,
  ariaAttrs
} = useButton(props);

// 样式类（可选，开发者可以完全自定义）
const buttonClass = computed(() => {
  return [
    props.class,
    {
      'is-pressed': isPressed.value,
      'is-focused': isFocused.value,
      'is-disabled': disabled
    }
  ];
});
</script>
```

### `src/components/button/index.ts`（导出）

```typescript
export { useButton } from './useButton';
export { default as Button } from './Button.vue';
export type { ButtonProps, UseButtonReturn } from './types';
```

## 🔄 与当前目录对比

### ❌ 当前结构（不适合组件库）

```
src/
├── main.js          ← 示例应用入口
├── counter.js       ← 示例代码
└── style.css        ← 全局样式
```

### ✅ 建议结构（符合 Headless UI 架构 - Vue 专用）

```
src/
├── composables/     ← 核心逻辑层（Composables）
├── components/      ← 组件目录（逻辑 + 可选默认渲染）
├── utils/           ← 工具函数
└── index.js         ← 库的入口文件
```

## 🚀 迁移建议

1. **第一步**：创建 `src/composables/` 和 `src/components/` 目录
2. **第二步**：将示例代码移到 `examples/` 目录
3. **第三步**：创建第一个组件（如 Button）作为示例
4. **第四步**：配置构建工具，支持 Vue 组件库构建

## 📦 构建配置建议

### `vite.config.ts` 示例（Vue + TypeScript 组件库）

```typescript
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { resolve } from 'path';
import dts from 'vite-plugin-dts'; // 自动生成类型定义文件

export default defineConfig({
  plugins: [
    vue(),
    dts({
      insertTypesEntry: true, // 生成类型入口文件
      outDir: 'dist', // 类型文件输出目录
    })
  ],
  build: {
    lib: {
      entry: resolve(__dirname, 'src/index.ts'),
      name: 'Leaf',
      fileName: (format) => `leaf.${format}.js`,
      formats: ['es', 'cjs', 'umd']
    },
    rollupOptions: {
      // 将 Vue 作为外部依赖，不打包进库
      external: ['vue'],
      output: {
        globals: {
          vue: 'Vue'
        }
      }
    }
  }
});
```

### `package.json` 配置示例（TypeScript）

```json
{
  "name": "leaf",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/leaf.cjs.js",
  "module": "./dist/leaf.es.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/leaf.es.js"
      },
      "require": {
        "types": "./dist/index.d.ts",
        "default": "./dist/leaf.cjs.js"
      }
    },
    "./style.css": "./dist/style.css"
  },
  "peerDependencies": {
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.2.4",
    "vite-plugin-dts": "^3.0.0",
    "vue-tsc": "^1.8.0"
  }
}
```

### `tsconfig.json` 配置示例

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "declaration": true,
    "declarationMap": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts", "src/**/*.vue"],
  "exclude": ["node_modules", "dist"]
}
```

## 💡 使用示例

### 方式一：只使用 Composable（完全自定义渲染）

**JavaScript 用户：**
```vue
<template>
  <button
    :class="['my-custom-button', { 'is-pressed': isPressed }]"
    @click="onClick"
  >
    自定义按钮
  </button>
</template>

<script setup>
import { useButton } from 'leaf';

const { isPressed, onClick } = useButton({
  onClick: () => console.log('clicked')
});
</script>
```

**TypeScript 用户（获得类型提示）：**
```vue
<template>
  <button
    :class="['my-custom-button', { 'is-pressed': isPressed }]"
    @click="onClick"
  >
    自定义按钮
  </button>
</template>

<script setup lang="ts">
import { useButton } from 'leaf';
import type { ButtonProps } from 'leaf';

const props: ButtonProps = {
  onClick: () => console.log('clicked')
};

const { isPressed, onClick } = useButton(props);
// ↑ IDE 会自动提示 useButton 的参数类型和返回值类型
</script>
```

### 方式二：使用默认组件（快速开始）

**JavaScript 用户：**
```vue
<template>
  <Button @click="handleClick">
    默认按钮
  </Button>
</template>

<script setup>
import { Button } from 'leaf';

const handleClick = () => {
  console.log('clicked');
};
</script>
```

**TypeScript 用户（获得类型提示）：**
```vue
<template>
  <Button @click="handleClick" :disabled="false">
    默认按钮
  </Button>
</template>

<script setup lang="ts">
import { Button } from 'leaf';

const handleClick = (event: MouseEvent) => {
  console.log('clicked', event);
};
// ↑ IDE 会自动提示 Button 组件的所有 props 类型
</script>
```

